generator:
  class: dmAdminDoctrineGenerator
  param:
    model_class: SidCabinetRecrutement
    theme: dmAdmin
    non_verbose_templates: true
    with_show: false
    route_prefix: recrutement
    with_doctrine_route: false
    config:
      actions:
        new:
          credentials: [[super_admin, admin]]
        delete:
          credentials: [super_admin]
        save:
          credentials: [[super_admin, admin]]
        history:
          credentials: [[super_admin, admin]]
        list:
          credentials: [[super_admin, admin]]
        _save_and_list:
          credentials: [ super_admin ]
        _save_and_next:
          credentials: [ super_admin ]
        _save_and_add:
          credentials: [ super_admin ]
        _view_page:
          credentials: [ super_admin ]
      fields:
        is_active:
          label: Active
      list:
        batch_actions:
          delete:
            credentials: [ super_admin ]  # il faut les permissions super_admin ET admin
          activate:
            credentials: [[ super_admin , admin ]]  # il faut la permission super_admin OU admin pour accéder
          deactivate:
            credentials: [[ super_admin , admin ]]  # il faut la permission super_admin OU admin pour accéder
        display:
          - '=title'
          - is_active
          - created_at
          - updated_at
#          - text
        sort:
          - position
          - asc
        table_method: getAdminListQuery
        table_count_method: ~
        sortable: true
      filter:
        display:
          - title
          - is_active
          - created_at
          - updated_at
#          - text
      form:
        display:
          Type de l'offre: [is_active]
          Titre de l'offre: [title]
          Text: [text]
        class: SidCabinetRecrutementAdminForm
        fields: {  }
      edit: ~
      new: ~