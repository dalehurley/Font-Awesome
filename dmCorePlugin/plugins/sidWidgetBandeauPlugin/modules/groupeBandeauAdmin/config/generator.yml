generator:
  class: dmAdminDoctrineGenerator
  param:
    model_class: SidGroupeBandeau
    theme: dmAdmin
    non_verbose_templates: true
    with_show: false
    route_prefix: groupe_bandeau
    with_doctrine_route: false
    config:
      actions: ~
      fields:
        is_active:
          label: Active
      list:
        display:
          - '=title'
          - is_active
          - created_at
          - updated_at
        sort:
          - position
          - asc
        table_method: getAdminListQuery
        table_count_method: ~
        sortable: false
      filter:
        display:
          - title
          - is_active
          - created_at
          - updated_at
      form:
        display:
          'Nom du groupe': [title, is_active]
          'La page du groupe': [dm_page_id]
        class: SidGroupeBandeauAdminForm
        fields: {  }
      edit: ~
      new: ~
